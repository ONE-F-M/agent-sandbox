# Copy to .env before running bake_image.sh — only needed for the local bake
# step (cloning the private ONE-F-M repos). Never used at Cloud Run runtime;
# runtime config is Secret Manager only, per deploy.py.

GITHUB_TOKEN=
